import React from "react";
import ReactDOM from"react-dom";

import App from "../../../App";

const rootElemet = document.getElementById("root");
ReactDOM.render(
    <React.StrictMode>
        <App />
    </React.StrictMode>,
    rootElemet

);
